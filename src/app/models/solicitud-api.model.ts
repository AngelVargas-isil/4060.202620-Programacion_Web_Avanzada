// Forma "cruda" que devuelve la API de práctica (JSONPlaceholder /users).
// Solo tipamos los campos que usamos.
export interface UsuarioApi {
  id: number;
  name: string;
  username: string;
  email: string;
}
